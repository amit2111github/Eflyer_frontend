import React from 'react';
import Base from '../core/Base';
const Profile = () => {
	return (
		<Base title="Profile Paage">
			<h1>This is user Profile</h1>
		</Base>
	);
};
export default Profile;
